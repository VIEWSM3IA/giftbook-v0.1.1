# Local development only. Run bash server/dev-db.sh first.
DATABASE_URL=postgresql://giftbook@127.0.0.1:55437/giftbook
NODE_ENV=development
ALLOW_LOCAL_LOGIN=true
PORT=43117
HOST=0.0.0.0
# Set PUBLIC_PREVIEW=true only behind Cloudflare Tunnel; store both secrets outside source control.
PUBLIC_PREVIEW=false
PREVIEW_PUBLIC_ORIGIN=https://giftbook.91341117.xyz
# PREVIEW_ACCESS_PASSWORD=
# PREVIEW_ACCESS_SECRET=
# Required for actual WeChat login; never commit real secrets.
WECHAT_APP_ID=
WECHAT_APP_SECRET=
